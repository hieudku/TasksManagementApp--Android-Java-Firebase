<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:orientation="vertical"
    tools:context=".MainActivity"
    android:background="@drawable/ocean_blue_copy_space_abstract_paper_waves"
    android:gravity="center_horizontal">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/taskflo"
        android:textColor="@color/title_dark_cherry"
        android:layout_marginTop="50dp"
        android:textSize="40sp" />

    <EditText
        android:id="@+id/email"
        android:layout_width="280dp"
        android:layout_height="48dp"
        android:minHeight="48dp"
        android:layout_marginTop="30dp"
        android:hint="@string/emailHint"
        android:textColor="@color/title_dark_cherry"
        android:textColorHint="@color/title_dark_cherry"/>

    <EditText
        android:id="@+id/password"
        android:inputType="textPassword"
        android:layout_width="280dp"
        android:layout_height="48dp"
        android:minHeight="48dp"
        android:hint="@string/passwordHint"
        android:layout_marginTop="8dp"
        android:textColor="@color/title_dark_cherry"
        android:textColorHint="@color/title_dark_cherry"/>

    <Button
        android:id="@+id/login_button"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:text="@string/loginButton"
        android:layout_marginTop="8dp"
        android:textColor="@color/grey"
        android:backgroundTint="@color/title_dark_cherry" />

    <Button
        android:id="@+id/register_button"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/registerButton"
        android:textColor="@color/grey"
        android:backgroundTint="@color/title_dark_cherry"/>

</LinearLayout>